{{>layout/header}}

<nav class="navbar navbar-expand-md navbar-dark bg-dark">
    <ul class="navbar-nav">
        {{#currentSection}}
            <li class="nav-item">
                <a href="/section/update/{{id}}" role="button" class="btn btn-dark center">
                    <span class="material-icons">drive_file_rename_outline</span>
                </a>
            </li>
        {{/currentSection}}
        <button type="button" class="btn btn-dark dropdown-toggle" data-toggle="dropdown">
            {{#currentSection}}
                {{name}}
            {{/currentSection}}
            {{^currentSection}}
                섹션
            {{/currentSection}}
        </button>
        <div class="dropdown-menu">
            {{#sectionList}}
                <a class="dropdown-item" href="/{{id}}">{{name}}</a>
            {{/sectionList}}
        </div>
        <li class="nav-item">
            <a href="/section/save" role="button" class="btn btn-dark center">
                <span class="material-icons">add_box</span>섹션
            </a>
        </li>
        <li class="nav-item">
            <a href="/category/save" role="button" class="btn btn-dark center">
                <span class="material-icons">add_box</span>카테고리
            </a>
        </li>
        <li class="nav-item">
            <a role="button" class="btn btn-dark center">
                <span class="material-icons">add_box</span>북마크
            </a>
        </li>
    </ul>
    <form class="form-inline ml-auto" action="">
        <input class="form-control mr-sm-2" type="text" placeholder="검색어를 입력하세요">
        <button class="btn btn-success" type="submit">검색</button>
    </form>
</nav>

<section class="category-section">
    <div id="category-container" class="card-columns">
        {{#category}}
            <!-- 카테고리 기본 폼 -->
            <div class="card" style="border-color: {{color}}">
                <!-- 컬러는 데이터 받아서 결정 -->
                <div class="card-header" style="border-color: {{color}}">
                    <a href="/category/update/{{id}}" style="color: {{color}}">{{name}}</a>
                    <span class="counter" style="color: {{color}}">{{openCount}}</span>
                </div>
                <!-- 별칭 표시 -->
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">
                        별칭
                        <span class="counter">카운트</span>
                    </li>
                    <li class="list-group-item">
                        별칭
                        <span class="counter">카운트</span>
                    </li>
                </ul>
            </div>
            <!-- 카테고리 기본 폼 END -->
        {{/category}}
    </div>
</section>

</div>

{{>layout/footer}}